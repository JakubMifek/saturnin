{
  "mcpServers": {
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"]
    },
    "fetch": {
      "type": "stdio",
      "command": "uvx",
      "args": ["mcp-server-fetch"]
    }
  },
  "_note": "Role-scoped MCP configuration is enforced at the launcher level; this file lists servers available to the runtime, not a blanket grant to every agent. See policies/mcp.yaml for per-role declarations."
}
